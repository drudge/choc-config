(name [slideDown (with callback)])

(trigger
    (tab .slideDown))

(only-in source.js.jquery)

(snippet [.slideDown(%{1/(^[0-9]+$)|.+/(?1::')/}%{1="slow/400/fast"}%{1/(^[0-9]+$)|.+/(?1::')/}, function() {
	%{0="//Stuff to do *after* the animation takes place"};
});
])