(name [slideUp (with callback)])

(trigger
    (tab .slideUp))

(only-in source.js.jquery)

(snippet [.slideUp(%{1/(^[0-9]+$)|.+/(?1::')/}%{1="slow/400/fast"}%{1/(^[0-9]+$)|.+/(?1::')/}, function() {
	%{0="//Stuff to do *after* the animation takes place"};
});
])