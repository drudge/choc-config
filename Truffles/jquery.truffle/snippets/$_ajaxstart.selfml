(name ajaxStart)

(trigger
    (tab .ajaxStart))

(only-in source.js.jquery)

(snippet [.ajaxStart(function() {
  %{1="//stuff to do when an AJAX call is started and no other AJAX calls are in progress"};
});
%{0}])