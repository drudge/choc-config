(name jQuery.each)

(trigger
    (tab jQuery.each))

(only-in source.js.jquery)

(snippet [jQuery.each(%{1="array/object"}, function(%{2="index"}%{3=", %{4="val"}"}) {
  %{0="//iterate through array or object"}
});])