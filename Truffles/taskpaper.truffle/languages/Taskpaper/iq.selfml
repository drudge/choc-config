(detector
    (extension todo)
    (extension tasks)
    (extension todolist)
    (extension taskpaper))

(in meta.task.pending.tasks)

(in meta.header-line.tasks
    (symbol-list
        (transformation s/:\s*$//)))

(in meta.task
    (symbol-list))